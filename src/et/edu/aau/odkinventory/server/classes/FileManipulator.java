/**
 * 
 */
package et.edu.aau.odkinventory.server.classes;

/**
 * @author Mahder on macbook Pro
 *
 */
import java.io.*;

public class FileManipulator {
	public static void generateXMLFile(String folerLocation,String fileName)
	throws Exception{
		/*String fullFileName = folerLocation+"/"+fileName;
		File file = new File(config.getServletContext().getRealPath("/")+ "/report/" + fileName)
		FileWriter fileWriter = new FileWriter(file);
		PrintWriter printWriter = new PrintWriter(fileWriter);
		printWriter.close();*/
	}
}//end class
